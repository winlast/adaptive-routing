# Состояние проекта

Документ для возобновления работы после перерыва. Контекст переписки не
переживает конец сессии — сохраняются только репозиторий и этот файл.

Последнее обновление: 20.09.2026.

## С ЧЕГО НАЧАТЬ В НОВОЙ СЕССИИ

Прочитать этот файл целиком — больше ничего восстанавливать не нужно.
Затем, если предстоит трогать числа или тексты:

```bash
python tools/verify_numbers.py    # 64 утверждения против data/
tools/test_web.sh                 # браузерная версия против Python
python tools/check_deck.py        # вёрстка презентации
```

Обе проверки должны проходить без замечаний. Если не проходят — значит
кто-то менял файлы мимо сборочных скриптов.

## ПЕРЕЛОМ 20.09: только продукт

Автор объявил смену направления дословно так: «мы полностью забываем то,
что делали для научных статей и конференций. сейчас на уме — только
реально крутой работающий продукт (для премии Леденцова)».

**Что это отменяет.** Прежний порядок «сначала научный результат, потом
статьи, потом питч» больше не действует. Снимаются и все ограничения,
которые до этого считались незыблемыми: зафиксированные названия статей,
лимиты в 2 и 6 страниц, обязательное совпадение оформления с поданными
файлами, аннотация ИИАСУ, которую не удалось заменить. Ничего из этого
больше не связывает работу.

**Очистка ветки `main` (20.09).** С продуктовой ветки убрано всё, что
относилось только к статьям и конференциям: папка `docs/` со статьями,
тезисами, презентацией доклада и программой конференции, а также
инструменты их сборки — `build_ri.py`, `build_ii.py`, `build_ii_v1.py`,
`docxbuild.py`, `estimate_pages.py`, `check_deck.py`,
`build_deck_conference.py`. Всё это **целиком сохранено на ветке
`research`**, проверено перед удалением.

`tools/verify_numbers.py` перестал перебирать `docs/*.docx` и проверяет
презентацию премии вместо конференционной. Утверждений по-прежнему 64:
проверки профилирования, чужого ПО, трассы Azure, модуля в nginx и
экономики сохранены. *При первой попытке правки вместе с циклом по
статьям случайно вырезались проверки PostgREST, Azure и экономики —
счётчик упал с 64 до 48 и это выдало ошибку. Правка переделана
точечно.*

README переписан как страница продукта: ведёт проблемой на чужой базе
данных, а происхождение из исследования ушло в короткий раздел в конце.
README ветки `research` **не тронут** — она заморожена целиком.

**Что это НЕ означает.** Ничего не удаляется безвозвратно. `docs/` со статьями и
презентацией доклада, ветка `research`, инструменты сборки документов в
`tools/` остаются в репозитории. Удаление необратимо, хранение ничего не
стоит, а просьба была прекратить работу над ними, а не уничтожить их.
Считать это архивом. Удалять — только если автор скажет прямо.

**Как теперь выбирать работу.** Задача берётся в работу, если она делает
продукт лучше, достовернее или проще во внедрении. Замеры по-прежнему
нужны — именно они делают продукт убедительным, — но делаются ради
продукта. Правила честности и запрет подгонять условия под нужный ответ
действуют без изменений.

**Что из этого следует прямо сейчас:**

- Вопрос «сливать ли `research` с `main`» больше не привязан к подаче
  статей. Прежний ответ («не сливать, пока статьи не отправлены»)
  основывался на посылке, которой больше нет.
- Переименование репозитория в `loadlens` стало не косметикой, а
  приведением названия в соответствие с тем, чем проект теперь является.
- README должен вести продуктом, а не происхождением из исследования.

**Что в работе, в этом порядке приоритета (состояние на 20.09):**

1. Премия Леденцова и Венчурные игры. Материалы готовы: заявка,
   презентация на 14 слайдов, ответы на вопросы, сценарий видео,
   раздаточная карточка с QR. Гипотеза проверена на чужом ПО и на
   производственной трассе Microsoft — см. Часть 6.
2. Продукт. **Направление 3 ниже устарело: большая его часть сделана.**
   Есть модуль для nginx, маршрутизатор на Go, упаковка в контейнеры,
   диагностика в браузере, английский README. Не сделано главное —
   журнал обращений настоящей компании.
3. Открытый вопрос, который стоит решить: **сливать ли ветку `research`
   с `main`** (см. «Направление 4») и **переименовывать ли репозиторий**
   из `adaptive-routing` в `loadlens`. Переименование сломает QR-коды на
   раздаточной карточке и адрес GitHub Pages — после него надо
   перегенерировать `ledentsov/раздатка/` и поправить ссылки.

**Железное правило, нарушение которого уже стоило ошибок в статьях:**
ни одно число не попадает в текст, пока не лежит в файле в `data/`.

## Порядок действий: как мы сюда пришли

Хронология решений, чтобы не переигрывать заново то, что уже решено.

1. **Исходный проект** — учебная работа о маршрутизации между Flask и
   FastAPI. Заявлен прирост 39–87 %.
2. **Проверка собственной работы** показала, что классификатор
   математически эквивалентен одному условию `if io_intensity >= 0.15`,
   а прирост получен на эмулированной нагрузке (холостой цикл).
   Результат признан бессодержательным.
3. **Полная переделка эксперимента**: настоящие вычисления, настоящие
   обращения к базе и по сети. Первая постановка (стоимость определяется
   маршрутом) оказалась вырожденной — обучению негде себя проявить.
4. **Вторая переделка**: стоимость запроса стала зависеть от параметра
   запроса, а не от маршрута. Это и дало настоящий результат.
5. **Серия замеров** — профилирование, точность оценок, Парето-граница,
   сравнение при равной ёмкости, перекрёстный эксперимент, уровни
   нагрузки, контроли на однородном потоке, цена решения, деградация.
6. **Статьи переписаны** под полученный результат, дважды: сначала под
   зафиксированные аннотации, затем — когда жюри сняло ограничение.
   Аннотацию тезисов поменять удалось, аннотацию ИИАСУ — нет, поэтому
   статья ИИАСУ существует в двух видах.
7. **Проект развёрнут в стартап**: вход через диагностику по журналам
   (`loadlens/`), а не через прокси; заявка и презентация к премии.
8. **Сверка чисел** выявила, что часть величин попадала в текст из
   вывода консоли, а не из файлов. Исправлено, добавлены проверки.

## Кратко: к чему пришли

Три утверждения, каждое подтверждено измерением с повторами.

1. **Маршрутизация между синхронным и асинхронным движком по оценке
   нагрузки каждого запроса строго лучше любого одиночного движка** —
   одновременно по пропускной способности и по хвосту задержек. Против
   «всё асинхронно»: +42 % запросов в секунду и в 13 раз меньше
   пострадавших лёгких запросов.
2. **Стоимость запроса не определяется его маршрутом.** Разброс внутри
   одного адреса — от 61 до 85 раз. Оценка по маршруту, на которой работают
   промышленные балансировщики с весами, ошибается на 80 %; оценка по
   признакам запроса — на 3.4 %.
3. **Обучение необходимо ровно в одной из двух ролей оценки.** Решение
   «пускать ли запрос в event loop» рукописное правило принимает не хуже
   модели. Взвешивание очереди требует верной абсолютной величины, и там
   ошибка правила стоит втрое большей доли пострадавших запросов.
   Проверено перекрёстным экспериментом на всех четырёх сочетаниях.

Третий пункт — прямой ответ на подзаголовок статьи для ИИАСУ («оценка
практической целесообразности») и самая содержательная часть работы.

## Структура репозитория

**Ничего ещё никуда не подано.** Жюри отправлены только аннотации;
сами статьи, заявка и презентация не отправлялись.

| Ветка | Назначение | Что внутри |
|---|---|---|
| `main` | **основная, стартап** | весь проект + `loadlens/` (продукт) + `ledentsov/` (заявка и презентация) |
| `research` | **научная** | тот же проект без бизнес-материалов, README написан для инженера |

Статьи в `docs/` побайтово одинаковы на обеих ветках. `main` —
надмножество `research`, различие ровно в бизнес-материалах и README.

| Тег | Что это |
|---|---|
| `science-v1.0` | научная версия: переработанный эксперимент и статьи |
| `prototype-v0` | первая версия прототипа, опровергнута переделкой; сохранена как история |

Прежние теги `v1.0` и `conference-ready` удалены: они утверждали
«Conference submission version», хотя ничего не подавалось, и указывали
на прототип, классификатор которого был эквивалентен одному условию.

## Зафиксированные названия (менять нельзя)

- Тезисы «Русский инженер»: «Адаптивная маршрутизация HTTP-запросов между
  синхронным и асинхронным веб-движками на основе классификатора нагрузки»
- Статья ИИАСУ: то же + «: оценка практической целесообразности»

Отсюда ограничение: **основной эксперимент должен быть на паре
sync + async**, поскольку в названии заявлены два движка. Пул процессов
остаётся в коде как расширение и по умолчанию отключён (`WORKERS`).

**Аннотации: разрешение частичное.** 19.09 жюри разрешило менять всё,
но на практике поменять удалось только аннотацию тезисов. **Аннотация
статьи ИИАСУ осталась исходной** — с числами «39–87 %» и «в 1,8–2,4
раза» со старого прототипа. Поэтому статья ИИАСУ существует в двух
видах:

| файл в `docs/` | аннотация | когда нужен |
|---|---|---|
| `Статья ИИАСУ Синявский СД.docx` | переписанная под измеренный результат | если аннотацию всё же удастся заменить |
| `Статья ИИАСУ Синявский СД v1 (исходная аннотация).docx` | **исходная, поданная** | **для отправки в текущем виде** |

Обе содержат одинаковые измеренные числа. Версия v1 дополнительно
включает раздел «О предварительной серии измерений», объясняющий, почему
приведённые значения меньше заявленных в аннотации: холостой цикл, на
котором получена предварительная серия, ставит асинхронный движок в тот
же режим, что чисто вычислительный поток (8,7 запроса в секунду против
25–27), и завышает оценку эффекта примерно вдвое. Без этого раздела
читатель увидит противоречие между аннотацией и таблицами.

Собирается скриптом `tools/build_ii_v1.py`. Объём: 5,93 страницы из 6.

Ничего ещё никуда не подано.

**Лимиты объёма, нарушать нельзя:** тезисы «Русский инженер» — 2
страницы, статья ИИАСУ — 6 страниц, считая шапку, таблицы и список
источников. Обязательные разделы тезисов по требованиям организаторов:
«Введение», «Методы и материалы; результаты», «Заключение», названия
набираются в подбор к тексту полужирным.

**Оформление менять нельзя:** Times New Roman 12 пт, межстрочный
интервал 1,5, поля 2 см, отступ первой строки 709 твипов, выключка по
ширине, висячий отступ 567 у источников. Эталон — сами файлы в `docs/`.

## Договорённости с автором работы

- Отвечать всегда по-русски.
- Не преувеличивать результат и не создавать ложного впечатления
  формулировками, технически верными, но вводящими в заблуждение.
  Отрицательные части результата писать прямо.
- Статьи — полноценные научные тексты, **не дневник разработки**.
  Процесс улучшений и перебор вариантов в них не фиксируется.
- Обход проверок на антиплагиат и на определение сгенерированного
  текста — отклонено, дважды, и остаётся отклонённым.
- ~~Порядок задач: сначала научный результат, затем статьи, затем
  бизнес-питч.~~ **Отменено 20.09:** только продукт для премии
  Леденцова, см. раздел «ПЕРЕЛОМ 20.09» в начале документа.
- В конце каждого ответа перечислять, что не сделано и что требуется от
  автора лично.

## Что представляет собой проект сейчас

Шлюз (`gateway.py`) принимает HTTP-запросы и по каждому решает, в какой
движок его отдать. Движков два (`workers/`): синхронный на потоках
(Flask, порт 8201) и асинхронный на event loop (FastAPI, 8202). Отдельно
поднимается внешний сервис (8100), к которому движки обращаются при
операциях ввода-вывода, — благодаря ему I/O в эксперименте настоящее.

Нагрузка настоящая (`core/workload.py`): PBKDF2, кодирование JPEG,
сортировка, чтение SQLite, HTTP-обращение наружу. Ни одного `sleep`
вместо работы. Шесть маршрутов; **стоимость запроса определяется его
параметром** (`limit`, `size`, `days`), а не адресом.

Все содержательные политики (`core/policies.py`) устроены одинаково и
различаются единственным — источником оценки нагрузки
(`core/cost_model.py`, `core/predictor.py`). Это сделано намеренно: так
измеряется вклад именно оценки, не смешанный с различиями в правиле
выбора.

# Часть 1. История: первая постановка и почему она отброшена

Этот раздел сохранён, чтобы не пройти тот же путь заново.

Исходно стоимость запроса определялась только его маршрутом: шесть
адресов, у каждого фиксированная цена. В такой постановке таблица
«маршрут → стоимость» точна по построению, и задача сводится к
арифметике из теории очередей с точным формульным решением. Выучивать
нечего, поэтому обучение неизбежно проигрывало простой арифметике.

Что было измерено тогда и осталось верным:

- Без конкуренции движки почти неразличимы (разница 1-3 %). Вся разница
  рождается под нагрузкой, из очередей и блокировок.
- Блокировка event loop измерена прямо: лёгкий запрос деградировал с
  25 мс до 1643 мс, когда в асинхронный движок попадала вычислительная
  задача.
- Учёт веса очереди (вместо счёта запросов штуками) поднимал
  балансировщик с 70 до 78 запросов в секунду.
- Обучаемая модель значимого преимущества перед простой балансировкой
  **не показывала**.

Методические ошибки, допущенные тогда и исправленные:

- Обучающие данные собирались случайной политикой, а модель работала в
  состояниях, которые создаёт сама. Смещение распределения устранено
  переходом к сбору с частичным исследованием (`ExploringPolicy`), а
  затем задача вовсе перестроена вокруг профилирования.
- Эксперимент с изменением условий замедлял общую внешнюю зависимость,
  отчего дорожали все движки одинаково и приспосабливаться было не к
  чему. Заменён на асимметричное ухудшение одного движка.
- Первая версия классификатора была математически тождественна правилу
  `if io_intensity >= 0.15`: два из трёх входных признаков на решение не
  влияли вовсе. Это и стало причиной полной переделки.

# Часть 2. Текущая постановка и её результаты

## Диагноз первой постановки

Задача была слишком простой для машинного обучения. Шесть маршрутов с
фиксированной стоимостью каждый — это задача из теории очередей с точным
формульным решением. Выучивать нечего, поэтому арифметика по таблице
неизбежно побеждала, а модель лишь приближала то же самое с шумом. Это
было следствием искусственного упрощения, а не свойством задачи.

## Что изменено

**Стоимость запроса определяется его параметром, а не маршрутом.** Клиент
передаёт `limit`, `size` или `days`; прокси видит это значение в строке
запроса, не заглядывая в бизнес-логику. Замеренный разброс внутри одного
маршрута:

| запрос | собственное время |
|---|---|
| `/api/search?limit=10` | 10 мс |
| `/api/search?limit=600` | **640 мс** |
| `/api/report/generate?days=1` | 11 мс |
| `/api/report/generate?days=75` | **921 мс** |

Разброс 61 раз у `/api/search` и до 85 раз у `/api/report/generate`.
*Прежде здесь стояли 658 мс и «65-70 раз» — величины из самого первого
замера, шедшего напрямую без сети, не сохранённые ни в одном файле.
Они же внесены в список запрещённых в `tools/verify_numbers.py`.* Следствия:

- любая таблица «маршрут → стоимость» заведомо неверна для отдельного
  запроса;
- оптимальный движок меняется внутри маршрута: `search?limit=10` это
  лёгкий I/O (место в асинхронном движке), `search?limit=600` это 405 мс
  вычислений, которые в event loop блокируют всех соседей;
- экспертное правило по маршруту физически не может развести эти случаи.

Показатели степени различаются по маршрутам: для выборок из базы
зависимость линейная, для обработки изображения и агрегации за период —
квадратичная. Поэтому правило «умножить на параметр», которое инженер
пишет руками, верно не везде.

Значения параметра в трафике непрерывны (логравномерное распределение со
смещением к малым значениям), а не выбираются из известного списка:
оценка стоимости обязана обобщать на не встречавшиеся значения.

## Измеренная асимметрия движков (профилирование)

Для каждой пары «запрос, движок» измеряются две величины: собственное
время выполнения и суммарная задержка, причинённая лёгким обращениям,
которые шли на тот же движок параллельно.

| запрос | sync своё / ущерб | async своё / ущерб |
|---|---|---|
| `/api/auth/verify` | 129 / **18** | 123 / **121** |
| `/api/report?days=75` | 938 / **296** | 945 / **683** |
| `/api/image?size=2200` | 249 / 215 | 241 / 238 |
| `/api/feed?limit=400` | 493 / 17 | 491 / 46 |

Собственное время почти не зависит от движка. Расходятся движки в том,
сколько вреда запрос причиняет соседям: в потоковом сервере операции,
освобождающие GIL (хеширование, кодек JPEG, SQLite), выполняются
параллельно с обработкой других запросов, в event loop — нет.

Величина, которую имеет смысл складывать по очереди, — **занятость**
(собственное время + причинённая задержка), а не собственное время.

## Две разные величины о запросе, которые нельзя смешивать

Профилирование измеряет для каждой пары «запрос, движок» собственное
время выполнения и задержку, причинённую лёгким обращениям, которые шли
на тот же движок параллельно. Первая версия меры ущерба брала **сумму**
задержек — это оказалось методической ошибкой. Сумма измеряет потерю
пропускной способности, и по ней движки почти не различаются: в
потоковом сервере планировщик переключает потоки каждые несколько
миллисекунд, поэтому общее отобранное время примерно то же. Различие
движков — в **блокировке**: в потоке лёгкий запрос продвигается
порциями и завершается быстро, в event loop он ждёт чужое вычисление
целиком. Поэтому мера ущерба заменена на наибольшую задержку одного
обращения.

После замены картина стала физически осмысленной (мс):

| запрос | sync своё / блок | async своё / блок |
|---|---|---|
| `/api/auth/verify` | 130 / **9** | 127 / **125** |
| `/api/report?days=75` | 939 / **268** | 937 / **638** |
| `/api/report?days=45` | 328 / 76 | 333 / 222 |
| `/api/feed?limit=400` | 495 / 9 | 491 / 10 |
| `/api/search?limit=600` | 640 / 401 | 652 / 427 |

*Значения сверены с `data/cost_grid_detail.json` 20.09. До этой сверки
в таблице стояли числа промежуточного прогона — те самые, которые автор
нашёл в статьях: у `/api/feed` 9 и 10 были переставлены местами, у
`/api/report?days=45` стояло 81 вместо 76. В статьях это исправили
тогда же, а здесь ошибка прожила ещё сутки. Документ, который новая
сессия читает первым, обязан сверяться вместе со статьями.*

Собственное время от движка почти не зависит. Блокировка зависит сильно
там, где вычисление освобождает GIL (хеширование, кодек, SQLite), и
слабо там, где оно выполняется чистым Python-кодом (сортировка в
`/api/search`).

Отсюда две величины у каждой оценки стоимости: `cost` — полная цена
запроса, `blocking` — время, на которое движок недоступен остальным.
Очередь взвешивается блокировкой: ожидание внешнего сервиса на 500 мс
движок не занимает и веса в очереди почти не имеет.

## Точность оценки стоимости (главный численный результат)

Все оценки настроены на одной выборке из 384 замеров со значениями
параметра из рабочего распределения; проверка на 156 отложенных замерах
с не встречавшимися значениями. Медианная относительная ошибка:

| оценка | цена запроса | блокировка |
|---|---|---|
| среднее по маршруту | 80.3 % | 53.6 % |
| линейная поправка (показатель = 1) | 11.2 % | 35.0 % |
| степенной закон (показатель выучен) | 3.7 % | 11.1 % |
| нейронная сеть | 3.4 % | 12.6 % |

Разбивка по маршрутам показывает, где именно выигрывает обучение:
`/api/image/thumbnail` — 261 % / 55 % / 3.4 %, `/api/report/generate` —
251 % / 58 % / 7.0 %. Это маршруты с квадратичной зависимостью, для
которых экспертное правило «пропорционально параметру» неверно.
Контрольный случай `/api/auth/verify` (стоимость от параметра не
зависит) даёт 1.3 % у всех оценок — методика измеряет то, что задумано.

Выученные показатели степени против истинных: 1.87-1.95 против 2.00 для
квадратичных маршрутов, 0.93-1.02 против 1.00 для линейных. Показатель
модели не сообщался, он выведен из времён ответа.

## Схема эксперимента

Главный замер построен вокруг вопроса, который соответствует названию
работы буквально: **классификатор решает, безопасен ли запрос для event
loop**. Политика допускает запрос в асинхронный движок, только если
предсказанная блокировка не превышает порога; синхронный движок
принимает всё. Среди допустимых движков выбирается менее занятый.

Порог — не настроечная константа, а параметр компромисса: при нуле весь
трафик уходит в синхронный движок, при бесконечности политика
вырождается в балансировку по весу очереди. Набор порогов даёт кривую
«пропускная способность против хвоста задержек лёгких запросов», и
сравниваются кривые целиком, а не отдельные точки — это снимает произвол
в выборе рабочей точки.

Различаются политики единственным — источником оценки блокировки
(среднее по маршруту, линейная поправка, степенной закон, нейронная
сеть, точная таблица профилирования как верхняя граница).

Для отсчёта в замер включены `all_sync`, `all_async` (маршрутизации нет)
и `least_conn` (балансировка, не различающая запросы).

## Качество классификатора без шума нагрузки (`classifier_quality.py`)

Нагрузочный замер даёт итог, но его разброс между прогонами велик. Тот
же вопрос задан напрямую: разыгрывается 40 000 запросов из рабочего
распределения, для каждого известна истинная блокировка event loop (из
таблицы профилирования), и классификатор с данным порогом решает,
пускать ли запрос в асинхронный движок.

**При фиксированном пороге** — избыточная блокировка, попавшая в event
loop сверх порога, мс на тысячу запросов:

| порог | среднее по маршруту | линейная поправка | степенной закон | нейронная сеть |
|---|---|---|---|---|
| 15 мс | 0 (но 8.2 % зря отклонено) | 46 | 394 | 397 |
| 30 мс | 0 (но 19.6 % зря отклонено) | 27 | 33 | 35 |
| 60 мс | **7520** | 522 | **21** | **21** |
| 120 мс | 9480 | 3674 | 513 | 505 |
| 250 мс | 3246 | 2680 | 59 | **14** |

**При равной доле трафика в асинхронном движке** (пороги подобраны так,
чтобы пропускная способность выравнялась по построению):

| доля | классификатор | блокировки, мс/1000 | самая тяжёлая пропущенная |
|---|---|---|---|
| 60 % | среднее по маршруту | 7340 | **421 мс** |
| 60 % | линейная поправка | 4745 | 11 мс |
| 60 % | обученная модель | 4749 | 18 мс |
| 70 % | среднее по маршруту | 13630 | **426 мс** |
| 70 % | линейная поправка | 6548 | 26 мс |
| 70 % | обученная модель | 6578 | 27 мс |
| 80 % | среднее по маршруту | 20054 | **622 мс** |
| 80 % | обученная модель | 10244 | 74 мс |

Решающий переход — от оценки по маршруту к оценке по запросу: вдвое
меньше блокировок при той же пропускной способности и в 8-16 раз
меньше самый тяжёлый прорвавшийся запрос. Именно он и создаёт хвост.

## Нагрузочный замер: Парето-граница (`benchmark_budget.py`)

Конкурентность 32, 500 запросов, 3 повтора на точку. Приведены режимы,
которых никто не превосходит сразу по обеим осям:

| режим | rps | лёгкие дольше 200 мс |
|---|---|---|
| весь трафик в асинхронный движок | 24.7 | 89.3 % |
| весь трафик в синхронный движок | 27.2 | 35.1 % |
| оценка по маршруту, порог 15 мс | 29.6 | 0.4 % |
| нейронная сеть, порог 15 мс | 31.1 | 3.1 % |
| нейронная сеть, порог 30 мс | 31.4 | 5.0 % |
| степенной закон, порог 30 мс | 35.1 | 6.9 % |
| линейная поправка, порог 60 мс | 38.7 | 14.4 % |
| least-connections | 44.1 | 44.4 % |

Два вывода:

1. **Любой режим с классификатором строго лучше обоих одиночных
   движков сразу по двум осям.** Степенной закон при пороге 30 мс:
   +42 % пропускной способности против «всё асинхронно» и в 13 раз
   меньше пострадавших лёгких запросов; +29 % и в 5 раз против «всё
   синхронно». Это прямое подтверждение гипотезы из названия работы.
2. **Оценка по запросу вытесняет оценку по маршруту почти отовсюду.**
   Из шести режимов «по маршруту» на границе остался один, самый
   консервативный. Остальные вытеснены: степенной закон при пороге
   30 мс даёт 35.1 rps при 6.9 %, оценка по маршруту в той же области —
   31.8 rps при 9.2 %, хуже по обеим осям.

Против least-connections — размен, а не победа: −20 % пропускной
способности за шестикратное снижение доли пострадавших лёгких запросов.
Так и следует написать, не выдавая это за чистый выигрыш.

Таблица прямого профилирования на Парето-границу не попала вовсе.
Называть её «верхней границей достижимого», как было в первой редакции,
неверно: она измеряет блокировку при одном сопутствующем запросе, а под
полной смешанной нагрузкой движок ведёт себя иначе. Формулировки в коде
исправлены.

## Сравнение при равной доле трафика (`benchmark_equal_share.py`)

Сравнивать классификаторы при одном пороге некорректно: порог означает
для разных оценок разное, и сравниваются разные рабочие режимы, а не
точность. Пороги подобраны так, чтобы доля трафика в асинхронном движке
была одинаковой.

Доля 70 %, 3 повтора по 500 запросов:

| классификатор | порог | rps | лёгкие p95 | лёгкие >200 мс |
|---|---|---|---|---|
| среднее по маршруту | 58 | 26.7 | 112 | 2.5 % |
| линейная поправка | 20 | 30.5 | 354 | 6.7 % |
| степенной закон | 15 | 29.9 | 182 | 4.6 % |
| нейронная сеть | 15 | **32.0** | **145** | **3.1 %** |

Перепроверка, 5 повторов по 600 запросов:

| классификатор | rps | лёгкие p95 | лёгкие >200 мс |
|---|---|---|---|
| линейная поправка | 33.7 ±6.2 | 415 | 7.5 % |
| степенной закон | 34.9 ±6.2 | 377 | 5.5 % |
| нейронная сеть | 35.4 ±5.7 | **158** | **2.6 %** |

Два независимых прогона дали одно направление и близкую величину:
обученная модель снижает хвост в 2.5 раза и долю пострадавших запросов
втрое при равной пропускной способности. Разброс по rps велик (±6), но
хвостовые показатели устойчивы.

## Противоречие между офлайн- и нагрузочным измерением, и его разрешение

Офлайн-анализ состава пропущенной в event loop работы при доле 70 %
показывает, что три оценки по запросу **неразличимы**:

| классификатор | сумма, мс/1000 | p90 | p99 | доля >50 мс |
|---|---|---|---|---|
| среднее по маршруту | 13630 | 28.4 | 251.9 | 4.78 % |
| линейная поправка | 6548 | 15.2 | 23.6 | 0 % |
| степенной закон | 6579 | 15.2 | 24.3 | 0 % |
| нейронная сеть | 6578 | 15.2 | 24.2 | 0 % |

То есть различие в нагрузочном замере возникает **не на допуске**.

Рабочая гипотеза: оценка применяется в двух ролях с разными
требованиями. Для допуска достаточно правильного **порядка** запросов
относительно порога — линейное правило монотонно по параметру и порядок
сохраняет. Для взвешивания очереди нужна верная **абсолютная величина**:
значения складываются между собой и сравниваются между движками, а
линейное правило ошибается на 11 % и систематически — на маршрутах с
квадратичной сложностью.

## Проверка механизма: где именно нужно обучение (ГЛАВНЫЙ ВЫВОД)

Две роли разведены по разным источникам оценки
(`split_<допуск>_<вес>_<порог>`), проверены все четыре комбинации.
5 повторов по 600 запросов, конкурентность 32:

| допуск | вес очереди | rps | лёгкие p95 | лёгкие >200 мс |
|---|---|---|---|---|
| линейное правило | линейное правило | 37.6 ±4.5 | 310 | 6.4 % |
| линейное правило | **модель** | 36.2 ±4.7 | **137** | **1.7 %** |
| модель | линейное правило | 36.7 ±4.9 | 417 | 7.2 % |
| модель | **модель** | 35.5 ±5.9 | **130** | **2.2 %** |

Результат разделяется строго по столбцу «вес очереди». Чем принято
решение о допуске, не влияет: 310 против 417 и 137 против 130 —
разнонаправленно и в пределах разброса. Всё определяет источник оценки,
которой взвешена очередь.

**Вывод, который и составляет содержание работы.** Оценка нагрузки
запроса используется в двух ролях, и требования к ней в них разные:

- Решение «безопасен ли запрос для event loop» требует только верного
  **порядка** запросов относительно порога. Любое монотонное по
  параметру правило справляется; рукописное работает не хуже обученного.
- Взвешивание очереди требует верной **абсолютной величины** в
  миллисекундах: значения складываются между собой и сравниваются между
  движками. Здесь 11-процентная ошибка линейного правила,
  систематическая на маршрутах с квадратичной сложностью, стоит втрое
  большей доли пострадавших лёгких запросов (6.8 % против 1.95 %) и
  хвоста в 2.7 раза длиннее.

То есть ответ на вопрос о практической целесообразности — не
«обучение помогает», а **где именно оно необходимо, а где достаточно
нескольких строк кода**.

## Как разрешился заявленный риск

Перед началом было записано: усложнение задачи не гарантирует победу
обучаемой модели, сильный конкурент — рукописное линейное правило, и
если модель его не обойдёт, это следует написать прямо.

Разрешилось так. В маршрутизации как таковой (решение о допуске) модель
рукописное правило **не обходит** — и это записано выше без смягчений.
Обходит она его во второй роли, при взвешивании очереди, и величина
эффекта втрое по доле пострадавших запросов. Обе половины результата
зафиксированы.

## Зависимость выигрыша от уровня нагрузки (`benchmark_load_levels.py`)

Разнородный поток, 400 запросов, 3 повтора на точку. Выигрыш считается
относительно **лучшего** одиночного движка, которым во всех точках
оказался синхронный.

| Конкурентность | Синхронный | Асинхронный | Классификатор | Δ к лучшему | Средняя задержка |
|---|---|---|---|---|---|
| 16 | 32,7 | 26,8 | 43,9 | +34,3 % | в 1,51 раза |
| 32 | 27,8 | 26,1 | 38,3 | +37,8 % | в 1,50 раза |
| 64 | 26,4 | 26,1 | 38,4 | +45,5 % | в 1,69 раза |
| 96 | 27,6 | 25,4 | 32,7 | +18,3 % | в 1,45 раза |
| 128 | 25,5 | 24,8 | 32,3 | +26,4 % | в 1,57 раза |

Итог: **+18…+46 % и в 1,3–1,7 раза**, максимум при конкурентности 64.
Дальше система насыщается по процессорному времени (два движка, два
ядра), и преимущество убывает, оставаясь положительным.

**Числа аннотации не воспроизводятся**, и это записано в статье прямо.
Заявленные 39–87 % и 1,8–2,4 раза получены на предварительной серии,
где вычисление моделировалось холостым циклом: он не освобождает GIL и
парализует асинхронный движок полностью. Контроль на чисто
вычислительном потоке воспроизводит тот режим — асинхронный движок там
выдаёт 8,7 запроса в секунду против 25–27 при настоящих операциях. В
статье это подано как самостоятельный методологический результат:
**эмуляция вычислительной нагрузки холостым циклом завышает оценку
эффекта примерно вдвое.** Такой ход согласован с автором работы.

## Контроль на однородном потоке (утверждение аннотации проверено)

Конкурентность 32, 3 повтора.

| Поток | Лучший одиночный | Классификатор | Балансировка |
|---|---|---|---|
| только ввод-вывод | асинхронный, 231,8 | 122–132 (−43…−47 %) | 166,5 (−28 %) |
| только вычисления | синхронный, 55,9 | 48,8 (−13 %) | 39,7 (−29 %) |

Аннотация утверждает «на однородной нагрузке выигрыша нет» —
подтверждено в усиленной форме: маршрутизация там не бесполезна, а
**вредна**. На вычислительном потоке классификатор проигрывает заметно
меньше балансировки, поскольку верно отказывается пускать вычисления в
цикл событий.

## Стоимость самого решения о маршрутизации (замер 19.09)

Отдельный замер: сколько стоит принять решение, без учёта проксирования.
Одно ядро, разогретый код, 20 000 повторов.

| источник оценки | одно решение | пропускная способность на ядре |
|---|---|---|
| экспертное правило | 1,3 мкс | ~790 000 решений/с |
| степенной закон | 1,6 мкс | ~640 000 решений/с |
| нейронная сеть | **174 мкс** | **~5 700 решений/с** |

Вывод, важный для продукта. Степенной закон — обучаемая оценка, но по
стоимости она практически бесплатна и узким местом не станет никогда.
Нейронная сеть в сто раз дороже и при нескольких тысячах запросов в
секунду сама становится ограничением.

Это меняет практический вывод работы. В замерах преимущество сети
проявлялось при взвешивании очередей (втрое меньше пострадавших
запросов), но теперь известна и цена: сто раз по времени решения. Для
боевого применения разумной оценкой выглядит степенной закон, а сеть —
там, где запас по производительности заведомо велик. Отдельно это не
проверялось: замера «степенной закон при взвешивании очереди против
сети» в перекрёстном эксперименте не было, там сравнивались правило и
сеть.

**Чего не измерено:** сквозные накладные расходы шлюза против прямого
обращения к движку. Все сравнения политик шли через один и тот же шлюз,
поэтому его стоимость входила во все числа одинаково и на сравнение не
влияла, но отдельно не выделялась.

## Изменение условий во время работы: граница применимости (отрицательный результат)

Проверялось, сохраняет ли классификатор преимущество, когда система
меняется после профилирования. В асинхронном движке запускается фоновая
нагрузка, отбирающая у него процессорное время (шумный сосед). Замер
`experiments/benchmark_drift.py`, конкурентность 32, 3 повтора,
300 запросов до ухудшения и 500 после.

| политика | rps до | лёгкие >200 мс до | rps после | лёгкие >200 мс после |
|---|---|---|---|---|
| оценки неизменны | 37.6 | 5.4 % | 35.3 | **86.8 %** |
| поправка по разности с очередью | 40.9 | 6.4 % | 30.0 | 65.8 % |
| состояние движка, сравнение по блокировке | 42.1 | 9.4 % | 28.5 | 61.5 % |
| состояние движка, сравнение по полной цене | 40.8 | 9.7 % | 26.0 | 50.5 % |
| least-connections | 44.7 | 46.6 % | 31.6 | **35.6 %** |

**Вывод отрицательный и его нельзя смягчать.** До ухудшения
классификатор даёт 5.4 % против 46.6 % у least-connections — преимущество
в 8.6 раза, что независимо подтверждает основной результат ещё на одном
прогоне. После ухудшения лучший из адаптивных вариантов даёт 50.5 % при
26.0 rps, тогда как least-connections — 35.6 % при 31.6 rps, то есть
**лучше сразу по обеим осям**. Заранее зафиксированный критерий («не
хуже least-connections») не выполнен ни одним вариантом.

Улучшение сигнала адаптации монотонно улучшает хвост
(86.8 → 65.8 → 61.5 → 50.5 %), но каждая ступень стоит пропускной
способности (35.3 → 30.0 → 28.5 → 26.0) и разрыв не закрывает.

Причина физическая. Ценность классификатора в том, что он не пускает
блокирующую работу в **исправный** event loop. Когда сам event loop
лишён процессорного времени, на нём страдают и запросы, которые ничего
не блокируют, а классификатор продолжает их туда пускать — их блокировка
по-прежнему мала, и формально они безопасны. Правильная реакция — вовсе
перестать пользоваться испорченным движком, и least-connections делает
это самопроизвольно, через рост очереди.

Отсюда формулировка границы применимости для статьи: **подход опирается
на то, что охраняемый движок остаётся таким, каким его измерили.
Изменение нагрузки на соседние ресурсы он переносит, потерю
процессорного времени самим асинхронным движком — нет.** Возможное
продолжение (не проверено, и выдавать за результат нельзя): сочетать
классификатор с балансировкой по загрузке так, чтобы длина очереди
перекрывала решение классификатора при деградации движка.

### Три ошибки постановки этого эксперимента

Каждая давала отрицательный результат, который говорил об эксперименте,
а не о методе. Записаны, чтобы не повторить в четвёртый раз.

1. Замедлялась общая внешняя зависимость — дорожали оба движка
   одинаково, относительный порядок не менялся, приспосабливаться было
   не к чему.
2. Ухудшался синхронный движок. Но политика опирается на оценку
   блокировки **асинхронного**, и от этого она не устаревает.
3. Поправка считала собственную стоимость как «замер минус оценка
   очереди». Под нагрузкой эта разность вбирает ожидание, не связанное с
   деградацией, и раздувалась одинаково у обоих движков (до x9.4 у
   движка, который не менялся). Заменено на нижний дециль отношения
   замера к ожидаемому собственному времени: у исправного движка около
   единицы, у испорченного — 2.36 против 1.06 у соседнего.

# Часть 3. Статьи: что сделано (АРХИВ)

> Работа над статьями и конференциями прекращена 20.09. Раздел сохранён
> как история и как описание того, что лежит в `docs/`. Начинать отсюда
> новую работу не нужно.

Обе статьи переписаны полностью под новый результат. Аннотации оставлены
дословно, шапка (УДК, заголовок, автор, организация, ключевые слова)
перенесена из прежних файлов без изменений.

**Тезисы «Русский инженер»** — 1,94 страницы из 2. Разделы: Введение;
Методы и материалы; результаты; Заключение. Источников 4.

**Статья ИИАСУ** — 5,94 страницы из 6. Разделы: Введение; Цель, задачи и
гипотеза; Синхронная и асинхронная модели обработки; Экспериментальный
стенд; Различие движков (табл. 1); Нагрузка запроса не определяется его
маршрутом; Оценка нагрузки по признакам запроса; Правило маршрутизации;
Результаты на разнородном потоке (табл. 2); Контроль на однородном
потоке; Локализация механизма эффекта (табл. 3); **Цена решения**;
Критерий целесообразности и граница применимости; Заключение.
Источников 9, все сохранены от прежней версии и проверены ранее.

**Редакция от 19.09 после снятия ограничения на аннотации.** Аннотации
переписаны под измеренный результат. Раздел, объяснявший расхождение с
прежними числами, убран — объяснять больше нечего; замечание о том, что
эмуляция холостым циклом завышает оценку вдвое, сохранено в описании
стенда как методический довод в пользу настоящей нагрузки. Добавлен
раздел «Цена решения»: степенная оценка 1,6 мкс против 174 мкс у сети,
отчего практическая рекомендация сместилась в пользу степенной.

## Презентация доклада

`docs/Презентация Русский инженер Синявский СД.pptx` — 8 слайдов,
собирается скриптом `tools/build_deck_conference.py`.

Оформление не воспроизводится вручную: из официального шаблона
`Регламент и требования/!4_Шаблон презентации.pptx` оставлены восемь
подходящих слайдов (номера 1, 5, 6, 9, 10, 11, 19, 22), а их содержимое
заменено с сохранением начертания. Так шрифты, цвета и фоны совпадают с
требуемыми точно. Состав повторяет пример конференции
(`!2_Пример презентации.pdf`): титул, проблема, находка, метод,
результаты, отдельный вывод, итоги, благодарность.

Использованы четыре рисунка из `figures/`: блокировка движков, точность
оценок, схема принятия решения, достижимая граница и перекрёстный
эксперимент.

Проверка вёрстки — `tools/check_deck.py`: ширина строк меряется
Liberation Sans (метрически совместим с Arial), текст переносится по
словам, проверяется, помещается ли он в блок и не накладываются ли
блоки. Открыть файл в этой среде нечем, поэтому проверка расчётом —
единственная возможная. **Всё равно нужно открыть в PowerPoint.**

## Инструменты для статей (`tools/`)

- `docxbuild.py` — сборка абзацев, таблиц и источников с разметкой,
  дословно повторяющей оригинал;
- `build_ri.py`, `build_ii.py` — тексты статей; запуск перезаписывает
  файлы в `docs/`;
- `estimate_pages.py` — оценка числа страниц без Word: ширина строки
  меряется Liberation Serif (метрически совместим с Times New Roman),
  таблицы считаются по высоте строк, а не по сумме ячеек. Откалиброван
  на прежних файлах: давал 1,99 и 5,51 страницы при лимитах 2 и 6.

Оценка приблизительная. **Вёрстку и число страниц обязательно проверить,
открыв файлы в Word.**

# Часть 4. Премия Леденцова: что сделано

`research-v2` влит в `ledentsov`. Устаревшая схема архитектуры и её
генератор удалены — они изображали правило `load > 50`, которого в
проекте нет. Конференционные материалы не тронуты.

## Стратегия: вход через диагностику, а не через прокси

Ключевое решение. Никто не поставит незнакомый прокси в путь боевых
запросов, поэтому продавать маршрутизатор «с порога» нельзя. Входом
служит **диагностика**: инструмент читает журнал обращений, ничего не
меняя, и показывает клиенту проблему на его собственных данных. Сначала
человек видит, что его балансировщик слеп, и только потом ему нужен наш
маршрутизатор.

Это не новый продукт, а второй результат исследования («стоимость не
определяется маршрутом, но предсказуема по параметрам»), завёрнутый так,
чтобы посторонний мог проверить его у себя за минуту.

## LoadLens Diagnose (`loadlens/`)

Один файл, **без зависимостей**, читает combined-формат nginx, JSON и
CSV. Отвечает на три вопроса: разброс стоимости внутри маршрута; чем он
объясняется; во сколько раз ошибается оценка по маршруту.

**Решённая методическая проблема.** В журнале рабочей системы измеренное
время включает ожидание в очереди, которое от параметров не зависит и
глушит сигнал: сначала параметр объяснял лишь 29–51 %. Исправлено
подгонкой по нижней огибающей — очередь способна время только
увеличить, поэтому самые быстрые обращения в каждой группе близких
значений параметра ближе всего к собственной стоимости. Стало 78–99 %.

**Проверка на демонстрационном журнале** (снят с нашего стенда,
длительности настоящие): инструмент самостоятельно восстановил
показатели степени 1,91 и 1,98 при истинном значении 2,00, то есть вывел
квадратичную сложность операций из одних времён ответа в логе nginx.

| файл | назначение |
|---|---|
| `loadlens/loadlens.py` | инструмент, 604 строки, только стандартная библиотека |
| `loadlens/make_demo_log.py` | сборка демо-журнала из замеров шлюза |
| `loadlens/demo_access.log` | 1200 обращений с настоящими длительностями |
| `loadlens/demo_report.html` | готовый отчёт |
| `loadlens/README.md` | краткое руководство |

Инструмент лежит в корне ветки `main`, а не внутри `ledentsov/`: это
продукт, а не экспонат к заявке. В `ledentsov/` осталось только
относящееся к самой премии — `ЗАЯВКА.md` и `presentation/`.

Запуск: `python loadlens/loadlens.py loadlens/demo_access.log --html r.html`

## Бизнес-материалы

`ledentsov/ЗАЯВКА.md` — проблема, почему не решается настройками, что
измерено, продукт в двух этапах, для кого, модель, **что доказано и что
нет**, запрос. Запрос сформулирован не как «дайте денег на рост», а как
ресурс на проверку гипотезы на журналах трёх-пяти живых сервисов.

`ledentsov/presentation/` — 12 слайдов, собираются скриптом
`build_deck.py` из кода: числа берутся из одного места и не могут
разойтись с замерами, а геометрия проверяется на наложения (отрисовать
файл в этой среде нечем). Отдельный слайд отведён **границам
применимости** — там, где подход не работает. Старая презентация с
утверждениями «99,7 % точность», «2 мс», «+39–87 %», «в 6,1 раза»
заменена целиком.

## Название

Официальное длинное название оставлено как было — автор не помнит,
подано ли оно в заявку. Короткое рабочее имя продукта — **LoadLens**;
проверка показала, что в нише балансировщиков и средств наблюдаемости
устоявшегося продукта с таким именем нет. Перед коммерческим
использованием нужна проверка по реестру товарных знаков.

# Часть 5. Состояние работ и что дальше

## Что сделано

**Эксперимент и замеры**

- [x] `core/workload.py` — стоимость зависит от параметра запроса
- [x] `workers/` — синхронный и асинхронный движки, внешний сервис,
      у обоих движков `/noise` для имитации шумного соседа
- [x] `core/cost_model.py`, `core/predictor.py` — пять источников оценки,
      две целевые величины (полная цена и блокировка)
- [x] `core/policies.py`, `core/queue_state.py`, `gateway.py` — единое
      правило выбора, раздельные оценки для допуска и веса очереди
- [x] `experiments/profile_costs.py` — блокировка по сетке параметров
- [x] `experiments/collect_cost_samples.py`, `train_cost_model.py`
- [x] `experiments/classifier_quality.py` — качество без нагрузки
- [x] `experiments/benchmark_budget.py` — Парето-граница
- [x] `experiments/benchmark_equal_share.py` — при равной ёмкости
- [x] `experiments/benchmark_load_levels.py` — уровни нагрузки и
      контроли на однородных потоках
- [x] `experiments/benchmark_drift.py` — деградация движка
- [x] `experiments/measure_endpoint_cost.py` — стоимость запроса и её
      вычислительная часть
- [x] `experiments/measure_decision_cost.py` — цена принятия решения
- [x] `experiments/plot_results.py`, `plot_architecture.py` — пять рисунков

**Тексты и материалы**

- [x] Тезисы и статья ИИАСУ переписаны под результат, в двух редакциях
      аннотации для ИИАСУ
- [x] Презентация доклада, 8 слайдов, из официального шаблона
- [x] `loadlens/` — диагностика по журналам, без зависимостей
- [x] `ledentsov/ЗАЯВКА.md`, `ledentsov/presentation/` — материалы премии
- [x] README ветки `main` переписан как страница продукта

**Инструменты контроля**

- [x] `tools/verify_numbers.py` — 64 утверждения против `data/`,
      включая все презентации и все публичные markdown-тексты
- [x] `tools/estimate_pages.py` — объём документов без Word
- [x] `tools/check_deck.py` — вёрстка презентации без просмотрщика
- [x] `tools/build_ri.py`, `build_ii.py`, `build_ii_v1.py`,
      `build_deck_conference.py` — сборка всех документов из кода

## Что осталось: три направления

### Направление 1. Конференции — всё готово, нужна отправка

Ничего ещё не подано; жюри получило только аннотации. Готово к отправке:

| файл в `docs/` | объём | чем отправлять |
|---|---|---|
| `Тезисы Русский инженер Синявский СД.docx` | 1,97 из 2 стр. | «Русский инженер» |
| `Статья ИИАСУ Синявский СД v1 (исходная аннотация).docx` | 5,93 из 6 стр. | **ИИАСУ — эту** |
| `Статья ИИАСУ Синявский СД.docx` | 5,94 из 6 стр. | запасная, если аннотацию удастся заменить |
| `Презентация Русский инженер Синявский СД.pptx` | 8 слайдов | доклад |

Осталось: **открыть в Word и PowerPoint**, проверить вёрстку и число
страниц, отправить. Запас по объёму меньше десятой доли страницы, а
проверка сделана моделированием, не Word'ом.

### Направление 2. Премия Леденцова — материалы готовы, нужна проверка гипотезы

Готово: `loadlens/` (диагностика), `ledentsov/ЗАЯВКА.md` (бизнес-описание),
`ledentsov/presentation/` (12 слайдов, собираются `build_deck.py`).

**Главный нерешённый вопрос всего направления:** встречается ли
обнаруженный разброс стоимости в реальных сервисах или это особенность
нашего стенда. Пока это не проверено, «проблема массовая» остаётся
гипотезой, и в материалах она так и названа.

Что нужно сделать — по убыванию важности:

1. **Прогнать `loadlens` на журналах трёх-пяти живых сервисов.** Требует
   только журнала, ничего не устанавливается. Доступ к журналам есть
   только у автора — через знакомых разработчиков, преподавателей,
   стажировки. Это единственный шаг, который нельзя сделать в этой среде.
2. Добавить в презентацию к премии раздел про цену решения (1,6 мкс
   против 166) — он появился позже, чем собиралась презентация.
3. Ответить на три открытых вопроса из заявки: целевой клиент, модель
   монетизации, что показывать как MVP.
4. Проверить название «LoadLens» по реестру товарных знаков.

### Направление 3. Настоящий продукт — частично сделано

**Раздел написан до 20.09 и ниже сохранён как план. Пункты 4 и 5
выполнены**: форма поставки есть (`nginx/`, `gorouter/`, `docker/`),
английский README написан. Пункт 2 выполнен наполовину: `loadlens`
подбирает модель по журналу, но передача её маршрутизатору не
автоматизирована — модель копируется файлом `data/power_law.json`.
Пункты 1 и 3 не сделаны.

Диагностика работает и её можно отдавать людям хоть завтра.
**Маршрутизатор — исследовательский прототип, клиент поставить его не
может.** Он жёстко привязан к стенду: два обработчика на конкретных
портах, шесть известных маршрутов, модель стоимости с наших замеров.

Чтобы получился продукт, нужно, по порядку:

1. **Настройка вместо зашитых значений** — описание обработчиков
   клиента, какой из них какого типа.
2. **Модель стоимости по журналу клиента.** Половина работы уже есть:
   `loadlens` умеет подбирать зависимость по журналу, остаётся отдать
   результат маршрутизатору.
3. **Всё, что требует боевая эксплуатация:** таймауты, повторы, проверки
   живости, шифрование, метрики, поведение при отказе обработчика.
   Ничего из этого нет.
4. **Форма поставки.** Сейчас это отдельный процесс на Python перед
   сервисами — добавляет сетевой прыжок и медленный. Правильная форма —
   модуль внутри того прокси, который у клиента уже стоит (Envoy, nginx):
   тогда лишнего прыжка нет, а расчёт добавляет 1,6 мкс к тому, что
   прокси и так делает.
5. **Английский README** — репозиторий решено открыть, а техническая
   аудитория международная.

Оценка объёма: недели работы, а не дни. Точнее сказать нельзя, не
считал.

### Направление 4. Репозиторий

Структура приведена в порядок, осталась отправка (см. «Что требуется от
автора лично»). Отдельно стоит решить, **сливать ли ветки**: сейчас
`main` — стартап, `research` — научная версия, и различие между ними
только в бизнес-материалах и README. Если наука закончена, ветки можно
свести к одной, но пока они разведены осознанно.

## Линия статьи, подтверждённая измерениями

1. Разделение трафика между двумя движками даёт выигрыш сразу по двум
   осям относительно любого одиночного движка — это подтверждение
   заявленной гипотезы.
2. Оценка нагрузки по маршруту структурно недостаточна, потому что
   стоимость определяется параметром запроса; нужна оценка по запросу.
3. Обучение необходимо не для решения о маршруте, а для взвешивания
   очередей. Это и есть ответ о практической целесообразности.

4. Граница применимости: преимущество сохраняется, пока охраняемый
   движок остаётся таким, каким его измерили. При потере им
   процессорного времени обычная балансировка по загрузке становится
   предпочтительнее — отрицательный результат, который должен быть в
   статье.

Против least-connections результат — **размен, а не победа**: −20 %
пропускной способности за шестикратное снижение доли пострадавших лёгких
запросов. Писать именно так.

## Сверка чисел в статьях с данными (`tools/verify_numbers.py`)

**Ошибка, которую нашёл автор работы, а не Claude.** Таблица блокировок
в статье была собрана по промежуточному прогону профилирования, тогда
как в `data/cost_grid_detail.json` лежал более поздний. Два значения у
`/api/feed` оказались переставлены местами (9 и 10), у
`/api/report?days=45` стояло 81 против 76 в данных. Кроме того, разброс
«65 раз» и времена 10/658 мс были взяты из самого первого замера, шедшего
напрямую без сети, и ни в одном сохранённом файле не лежали.

Причина одна: числа переносились в текст из вывода в консоль, а не из
файлов. Вывод в консоль живёт до конца сессии, файл — всегда.

Исправлено:

- добавлены `experiments/measure_endpoint_cost.py` (собственная
  стоимость запроса и её вычислительная часть, без сети) и
  `experiments/measure_decision_cost.py` (цена принятия решения); оба
  пишут результат в `data/`;
- все числа в обеих статьях заменены на значения из сохранённых файлов;
- добавлен `tools/verify_numbers.py`, который сверяет 64 утверждения
  статей с данными и заодно ищет в тексте устаревшие числа («39–87»,
  «99,7», «658», «123 мс», «65 раз», «174 мкс»). Запускать перед каждой
  отправкой.

**Правило на будущее: ни одно число не попадает в текст, пока не лежит
в файле в `data/`.**

## Подводные камни, на которые уже наступили

- `pkill -f "gateway.py"` убивает и управляющий процесс, и оболочку, в
  командной строке которых встречается та же подстрока. Останавливать
  только по идентификатору процесса.
- Журнал запросов дописывался в файл со старой схемой колонок, из-за
  чего значения разъехались относительно заголовка и разбор молча давал
  неверные числа. В `gateway.py` добавлена проверка заголовка.
- Фоновый замер переживает обрыв оболочки и продолжает работать,
  перезапуская шлюзы и мешая следующим замерам. Проверять `pgrep`.
- Переобучение сети порождает другую модель, и опубликованные числа
  перестают ей соответствовать. Начальные веса зафиксированы, а
  `train_cost_model.py --saved` только оценивает уже обученную модель.
- Числа попадали в тексты из вывода консоли, а не из файлов, и разошлись
  с сохранёнными данными. Исправлено, добавлена сверка; правило записано
  в начале документа.
- Открытый в Word документ создаёт рядом файл `~$…`. Если пересобрать
  документ, пока он открыт, Word при сохранении затрёт сборку. Перед
  пересборкой документ надо закрыть.
- Блоки в шаблоне презентации рассчитаны на короткие подписи;
  содержательный текст из них вылезает. `tools/check_deck.py` это ловит.

## Как воспроизвести

**Замеры.** Порядок обязателен: профилирование задаёт эталон, сбор
замеров даёт обучающую выборку, и только после этого оценки доступны
политикам.

```bash
source venv/bin/activate
python workers/stub_service.py &     # внешний сервис, порт 8100
python workers/sync_worker.py &      # синхронный движок, 8201
python workers/async_worker.py &     # асинхронный движок, 8202

python experiments/profile_costs.py          # блокировка по сетке
python experiments/collect_cost_samples.py   # замеры для оценок
python experiments/train_cost_model.py       # настройка и проверка
python experiments/measure_endpoint_cost.py  # стоимость запроса
python experiments/measure_decision_cost.py  # цена решения
python experiments/classifier_quality.py     # качество классификатора
python experiments/benchmark_budget.py       # Парето-граница
python experiments/benchmark_equal_share.py  # при равной ёмкости
python experiments/benchmark_drift.py        # деградация движка
TRAFFIC=mixed LEVELS=16,32,64,96,128 python experiments/benchmark_load_levels.py
TRAFFIC=io_only LEVELS=32 python experiments/benchmark_load_levels.py
TRAFFIC=cpu_only LEVELS=32 python experiments/benchmark_load_levels.py
python experiments/plot_results.py           # пять рисунков
```

Погасить всё:
`ps aux | grep -E "[w]orkers/|[g]ateway.py" | awk '{print $2}' | xargs -r kill -9`

**Документы.** Собираются из кода, править .docx и .pptx руками нельзя —
при следующей сборке правки пропадут.

```bash
python tools/build_ri.py                 # тезисы
python tools/build_ii.py                 # статья, новая аннотация
python tools/build_ii_v1.py              # статья, исходная аннотация
python tools/build_deck_conference.py    # презентация доклада
python ledentsov/presentation/build_deck.py   # презентация премии

python tools/verify_numbers.py           # сверка чисел с data/
python tools/estimate_pages.py docs/*.docx
python tools/check_deck.py
```

**Диагностика LoadLens.**

```bash
python loadlens/loadlens.py loadlens/demo_access.log --html report.html
python loadlens/make_demo_log.py   # пересобрать демо-журнал из замеров шлюза
```

## Ограничения, которые нельзя забывать и нельзя замалчивать

- Каждый движок — один процесс, то есть около одного ядра из
  шестнадцати. Так устроен CPython; в проде масштабируют числом
  процессов, что умножает ёмкость, но не меняет эффекта внутри процесса.
- Разброс пропускной способности между прогонами достигает ±6 запросов
  в секунду. Выводы — только по нескольким повторам; хвостовые
  показатели устойчивее среднего.
- Таблица прямого профилирования **не является** верхней границей
  достижимого: она снята при одном сопутствующем запросе, а под полной
  смешанной нагрузкой движок ведёт себя иначе. В замерах она на
  Парето-границу не попала. Называть её границей — ошибка, которая уже
  была допущена и исправлена.
- Все замеры на одной машине (WSL2), диск и сеть общие.
- Где обучение должно было бы стать необходимым, но не проверено: когда
  стоимость зависит не от одного известного параметра, а от нескольких
  признаков запроса с взаимодействиями или от категориальных значений.
  Это направление для продолжения, а не доказанный результат.

## Что требуется от автора лично

Claude не может сделать это ни при каких условиях — нет доступа.

1. **Отправить на GitHub.** Обычный пуш; принудительный больше не
   нужен, переписывание истории закончено. Веток стало три.

   ```bash
   git fetch --all --prune          # иначе сведения об origin устарели
   git push origin main
   git push origin gh-pages         # публичная страница диагностики
   ```

   **Проверять состояние только после `fetch`:** `git log origin/main..main`
   читает локальную копию, и без обновления она врёт.

2. **Включить GitHub Pages** (Settings → Pages → ветка `gh-pages`, папка
   `/ (root)`) и убедиться, что страница открывается: на неё ведёт
   QR-код раздаточной карточки.

3. **Открыть документы в Word и PowerPoint** и проверить вёрстку и число
   страниц. Оценка сделана моделированием, Word может свёрстывать иначе,
   а запас меньше десятой доли страницы.

4. **Отправить материалы** на порталы конференций, подписать бумаги.

5. **Достать журналы обращений реальных сервисов** для проверки
   `loadlens`. Остаётся главным нерешённым вопросом: проверка на чужом
   ПО и на трассе Microsoft сняла возражение «это особенность вашего
   стенда», но боевого журнала конкретной компании у проекта по-прежнему
   нет. Доступ к таким журналам есть только через живых людей.

# Часть 6. Подготовка к Венчурным играм (премия Леденцова)

Задача, поставленная автором: показать проект так, чтобы он не выглядел
как «двухчасовой вайбкодинг», и при этом ничего не подгонять — «цель
сделать реально крутой и работающий продукт, а не всё надуманно».
Отсюда порядок: сначала снять возражения к достоверности, потом
упаковка, потом материалы питча.

## Проверка на чужом ПО: PostgREST (сделано 20.09)

Стенд: PostgreSQL 16 с таблицей на 300 000 строк, поверх неё PostgREST
12, перед ним OpenResty с журналом `$request_time`. Нашего кода в пути
запроса нет. Трафик — пять форм запросов, `limit` из лог-равномерного
распределения 1..20000.

Файлы: `thirdparty/`, результат — `data/thirdparty_postgrest.json`,
разбор — `thirdparty/RESULT.md`.

Подтвердилось: разброс внутри маршрута `/orders` **в 27 раз**, ошибка
оценки по маршруту 177 % против 33 % по признакам запроса, 10 % тяжёлых
обращений занимают 53 % времени.

**Не подтвердилось и привело к переделке инструмента:** один параметр
объяснял здесь лишь **18 %** разброса против 78–99 % на нашем стенде.
Причина не в методике, а в том, что у настоящего интерфейса стоимость
определяют несколько вещей сразу — число строк, сортировка, фильтр,
набор полей. LoadLens выбирал единственный «лучший» параметр.

Добавлена оценка сразу по всем видимым признакам (`fit_multi`,
`predict_multi` в `loadlens/loadlens.py`). Нечисловые параметры входят
признаком присутствия, перечисления — своей длиной. Обе оценки
настраиваются на одной половине обращений и проверяются на другой:
**56,9 % ошибки у одного параметра против 38,6 % у всех признаков**,
объяснённая доля выросла с 18 % до 33 %.

**Поправка в расчёте ошибки (20.09).** При переносе разбора в браузер
JavaScript и Python разошлись на 0,7 процентного пункта. Причина
оказалась не в переносе: `median_ape` делила на `max(a, 1e-9)`, и
обращения с длительностью «0,000 с» давали ошибку порядка 10¹¹ %. Ноль
в журнале nginx означает время ниже разрешения записи, а не отсутствие
работы, поэтому такие обращения исключены из расчёта точности в обеих
реализациях. На журнале PostgREST их 11 из 1500, на нашем
демонстрационном — ни одного, поэтому числа стенда не затронуты.

Поправка сдвинула числа в обе стороны: ошибка по признакам 33,8 → 33,1,
но и ошибка по одному параметру на отложенной половине 57,7 → 56,9.
Выигрыш от многопризнаковой оценки стал чуть скромнее. Это важно: если
бы исправление двигало числа только в нашу пользу, его следовало бы
перепроверить отдельно.

Инструмент сам, из одного журнала, нашёл, что у PostgREST дороже всего
сортировка: коэффициент +2,44, то есть примерно в 11 раз, тогда как
`limit` даёт всего +0,21. Это правда о PostgreSQL на неиндексированной
сортировке, и она не закладывалась в стенд.

## Проверка посылки на производственной трассе Azure (сделано 20.09)

Оставалось возражение, что стенды всё равно поднимали мы. Проверено на
трассе Azure Functions 2019 (Microsoft Research, USENIX ATC'20):
`thirdparty/azure_trace.py`, результат — `data/azure_trace.json`, разбор
— `thirdparty/AZURE.md`.

**32 968 функций, 1,8 млрд вызовов.** Разброс p99 к медиане внутри одной
функции: у половины — от 8 раз, у четверти — от 29, у десятой части — от
125. Доля функций с разбросом от 10 раз — **44,6 %**, от 100 раз — 11,7 %.

Что этим НЕ доказано и должно проговариваться прямо: параметров запросов
в трассе нет, поэтому предсказание по ним здесь не проверяется; типичная
функция расходится в 8 раз, а не в 61, как на нашем стенде с намеренно
тяжёлыми операциями. Числа 61 и 85 с нашего стенда — это специально
подобранные операции, выдавать их за обычное положение дел нельзя.

Архив трассы (55 МБ, чужая лицензия) в репозиторий не кладётся, ссылка и
команда — в `thirdparty/AZURE.md`.

## Упаковка стенда в контейнеры (сделано 20.09)

`docker/run.sh` поднимает весь стенд: внешний сервис, два движка и
демонстрацию с маршрутизатором внутри. Кроме Docker ничего не нужно.
Образ без `torch`/`pandas`/`scikit-learn` — оценки уже настроены.

Адреса соседей и интерфейс связывания вынесены в окружение
(`SYNC_URL`, `ASYNC_URL`, `STUB_URL`, `BIND_HOST`, `GATEWAY_HOST`).

**Числа в контейнерах слабее:** доля подтормаживающих запросов
56 / 18 / 4 % против 86 / 38 / 7 % на той же машине без Docker. Сетевой
мост добавляет постоянную задержку. Для питча это нормально — порядок
сохраняется, — но выдавать эти числа за числа статей нельзя.

## Маршрутизатор внутри nginx (сделано 20.09)

`nginx/loadlens.lua` — та же политика бюджета блокировки, выполняемая
внутри nginx, без отдельного шлюза и без лишнего сетевого перехода.
Ставится тремя файлами, пересборка nginx не нужна.

Узел выбирается переменной в `proxy_pass`, а не через `balancer_by_lua`:
balancer принимает адрес только числом и не разрешает доменные имена.
Занятость движков — в `lua_shared_dict`, потому что рабочих процессов у
nginx несколько.

Замер (`nginx/benchmark.py`, данные `data/nginx_module.json`): доля
лёгких обращений медленнее 200 мс **36,9 % против 0,0 %**, медиана
103,9 против 11,3 мс, пропускная способность 67,7 против 53,4 rps.

**Первый прогон пришлось отбросить:** рядом работал контейнер живой
демонстрации и гнал собственный поток в те же движки. Стенды надо
останавливать перед замером — в `thirdparty/run.sh` не было команды
`stop`, добавлена.

## Ёмкость при обещании по отклику и экономика (сделано 20.09)

`nginx/capacity.py`, данные `data/nginx_capacity.json`. Кривая
«нагрузка против p95 лёгких обращений» для обоих способов.

У `least_conn` p95 растёт с 83 до 998 мс при росте нагрузки. У нашей
маршрутизации остаётся в пределах **68–80 мс на всём диапазоне**:
тяжёлые обращения в цикл событий не допускаются, и лёгким не за кем
стоять.

| обещание | `least_conn` | наш модуль |
|---|---|---|
| p95 ≤ 100 мс | 37,8 rps | **59,2 rps (+57 %)** |
| p95 ≤ 200 мс | 62,3 rps | 59,2 rps (−5 %) |

Значения `least_conn` интерполированы между замеренными точками, это
помечено в данных полем `ёмкость_интерполирована`.

Экономика (`tools/unit_economics.py`, `data/unit_economics.json`,
разбор — `ledentsov/ЭКОНОМИКА.md`): при 1000 rps и обещании p95 ≤ 100 мс
нужно 17 экземпляров вместо 27, экономия **656 640 ₽ в год (−37 %)**.
Цены — публичный прейскурант Yandex Cloud, проверен 20.09.2026. Размер
экземпляра (4 vCPU, 8 ГБ) — предположение, отношение числа экземпляров
от него не зависит.

**При обещании 200 мс экономии нет вовсе — ноль рублей.** Это главное
ограничение продукта, и из него следует, кому его продавать не надо:
пакетной обработке, выгрузкам, очередям задач.

## Разбор конкурентов (сделано 20.09)

`ledentsov/КОНКУРЕНТЫ.md`. Главное, что нельзя прятать: **идея
предсказывать стоимость запроса до обработки не нова.** В обслуживании
языковых моделей она промышленно работает (предсказание длины ответа до
генерации), а Envoy через протокол ORCA пересчитывает веса по отчётам
самих сервисов.

Наше место сформулировано узко: модель стоимости выводится из обычного
журнала обращений, без изменений в сервисе, и применяется к разнородным
веб-обращениям, где признак стоимости заранее неизвестен и его надо
найти.

## Диагностика в браузере и публичная страница (сделано 20.09)

`loadlens/web/` — тот же разбор на JavaScript. Страница самодостаточна,
открывается двойным щелчком, сервера у неё нет, журнал никуда не
уходит. Собирается `python3 tools/build_web.py`.

Обязательное условие: **числа обязаны совпадать с версией на Python.**
Проверка `tools/test_web.sh` сравнивает оба разбора по значениям на двух
журналах и отдельно проверяет, что собранная страница строит отчёт. Node
в системе не нужен, всё идёт в контейнере. Расхождений нет.

Создана ветка **`gh-pages`** с собранной страницей. После включения
GitHub Pages в настройках репозитория страница будет доступна по адресу
https://winlast.github.io/adaptive-routing/ — это бесплатный постоянно
работающий публичный стенд для диагностики (живая демонстрация
маршрутизации туда не помещается: ей нужен сервер).

## Материалы к Венчурным играм (сделано 20.09)

- `ledentsov/ВОПРОСЫ_И_ОТВЕТЫ.md` — неудобные вопросы и честные ответы,
  включая раздел «чего говорить нельзя».
- `ledentsov/ВИДЕО_60_СЕКУНД.md` — посекундный сценарий.
- `ledentsov/раздатка/` — карточка A6 и два QR-кода. Коды **раскодированы
  и проверены**, ведут на страницу диагностики и на репозиторий.
- В презентацию добавлены два слайда: проверка на чужом ПО и экономика.
  Стало 14 слайдов, проверка геометрии наложений не находит.

## Ошибка с завышенным числом и как она обнаружилась (20.09)

Разовый `curl` к PostgREST дал разницу **в 118 раз**, и это число успело
попасть в инструкцию, в презентацию и в карточку. При семи повторах с
медианой (`thirdparty/handmade.py`, данные `data/handmade.json`) разница
оказалась **в 72 раза**: разовый замер попал на непрогретую базу.
Исправлено везде, «118 раз» внесено в список запрещённых величин.

Заодно выяснилось, что `tools/verify_numbers.py` просматривал только
`docs/*.pptx`, и в презентации к премии Леденцова спокойно жили три
устаревших числа: 658 мс, «65 раз» и 123 мс вместо 125. Проверка
расширена на все презентации и все публичные markdown-тексты, новые
числа привязаны к файлам. **Утверждений стало 80 вместо 48.**

Вывод, который стоит помнить: правило «ни одно число не попадает в
текст, пока не лежит в файле» работает только вместе с проверкой,
которая видит все тексты. Каждый новый публичный файл надо добавлять в
список в `verify_numbers.py`.

## Маршрутизатор на Go и сбор отчётов (сделано 20.09)

`gorouter/` — та же политика третьей реализацией. Сквозной замер
(`data/gorouter.json`) дал то же поведение, что модуль в nginx: 0,0 %
лёгких обращений медленнее 200 мс, медиана 11,1 мс против 11,3.
Совпадение важно само по себе: разойдись оно — результат держался бы на
частности реализации.

Стоимость решения: **58 нс без выделений памяти** в изолированном
замере, **около 1,2 мкс** под нагрузкой (туда входят состязание горутин
и два обращения к таймеру, которыми ведётся замер). Даже по верхней
оценке — десятая доля процента ядра при 1000 запросах в секунду.

Сбор отчётов (`loadlens/web/`): ничего не отправляется само,
пользователь видит готовый текст и решает сам, в сводку попадают только
числа. Отсутствие адресов и параметров проверяется автоматически.

`README.en.md` — сокращённый перевод. **Основным остаётся русский
README**, английский на него ссылается как на главный.

## Ловушки, в которые снова наступили (20.09)

- **`pkill -f` опять убил собственную оболочку** — шаблон совпал с
  командной строкой обёртки. Убивать только по PID или по порту через
  `ss -tlnp`.
- **Блок проверок вставился дважды**, потому что опорный комментарий
  встречался в файле два раза. Перед вставкой по якорю надо убеждаться,
  что якорь единственный.
- **Журнал `thirdparty/logs/access.log` менялся между прогонами**: через
  прокси идут все обращения, включая `handmade.py`. Из-за этого
  расхождение реализаций сначала не воспроизводилось.
- **Имя свойства со знаком процента в JavaScript** без кавычек — не
  предупреждение, а синтаксическая ошибка: страница не выполняется
  целиком. Поймала только сквозная проверка страницы, не сборка.

## Репозиторий: наведение порядка 20.09

**Авторство.** Во всей живой истории один автор — `winlast
<winlast09@mail.ru>`. Трейлеров `Co-Authored-By` в `main`, `research` и
`gh-pages` нет. Резервные ссылки `refs/original/*`, оставшиеся от
прежней перезаписи истории и хранившие старые коммиты с соавторством,
удалены; на сервер они никогда не отправлялись.

**Ошибка с адресом почты.** Ветка `gh-pages` была создана с адресом
`winlast099@gmail.com` вместо `winlast09@mail.ru`. GitHub связывает
коммиты с аккаунтами по адресу, и это дало бы второго участника в
разделе Contributors. Ветка пересобрана с верным адресом; при отправке
нужен `git push --force origin gh-pages` — один раз.

**Релиз.** На GitHub есть один релиз — «Conference submission version»,
и он **в состоянии черновика** (Draft). Черновик виден только владельцу
репозитория, посторонним его не показывают, поэтому вреда он не нанёс.
Это остаток той же истории, из-за которой удалялись теги `v1.0` и
`conference-ready`: они утверждали подачу на конференцию, которой не
было. Черновик подлежит удалению — тем более что конференционное
направление закрыто. Удаление черновика не трогает теги.

**Теги.** Их два, оба на сервере. `science-v1.0` оставить: он помечает
ту же точку, что хранит ветка `research`. `prototype-v0` удалить — он
указывает на версию, центральное утверждение которой было опровергнуто,
и в продуктовом репозитории это мусор. Релизы проверить из этой среды
нечем (`gh` не установлен), смотреть на сайте.

**Размер `.git`.** Был 3,1 ГБ из-за остатков перезаписи истории и
объектов удалённой ветки. После удаления `refs/original/*` и
`git gc --prune=now --force` стал **9,9 МБ**. `git fsck` повреждений не
находит.

### Ошибка, которую нельзя повторять: уничтожение venv

Чтобы исправить адрес автора в `gh-pages`, ветка создавалась
**в рабочей директории проекта**: `git checkout --orphan`, затем
удаление всех файлов, кроме `.git` и `venv`. Вместе со всем остальным
удалился и `.gitignore`, после чего `git add -A` забрал в коммит весь
`venv` — 39 051 файл. При возврате на `main` git удалил эти файлы как
отслеживаемые, и виртуальное окружение было уничтожено.

Восстановлено из `requirements.txt`; заодно в него добавлены
`python-pptx`, `qrcode` и `pillow`, которые ставились по ходу работы и
в файл не попадали — без них не собираются презентация премии и
раздаточная карточка.

**Правило:** любые операции с ветками, требующие очистки каталога
(`checkout --orphan`, массовое удаление файлов), выполнять **только в
отдельном рабочем дереве** через `git worktree add`, никогда в рабочей
директории проекта. Первая версия `gh-pages` была сделана именно так и
прошла без последствий — отход от этого порядка и стоил окружения.

### Очистка проверки чисел: чуть не вырезали лишнее

При удалении из `tools/verify_numbers.py` цикла по статьям вместе с ним
удалились проверки PostgREST, Azure, модуля в nginx и экономики: опорный
комментарий, по которому определялся конец блока, стоял дальше, чем
предполагалось. Счётчик упал с 64 утверждений до 48, и это выдало
ошибку. Правка переделана точечно, с проверкой `assert` на точное
совпадение вырезаемого текста.

**Правило:** при удалении блока по двум меткам проверять, что между
ними нет ничего постороннего, и сверять счётчик проверок до и после.

## Состояние диалога на конец 20.09

Что автор просил и что из этого следует помнить в следующей сессии.

**Модель.** Автор спрашивал, не переключиться ли с Opus на Sonnet ради
экономии лимита. Ответ был дан честный: контекст при переключении не
теряется, на механической работе разница мала, на поиске методических
ошибок — заметна. Решение осталось за автором.

**Управление стендами.** Автор потребовал, чтобы Claude сам следил за
контейнерами и серверами и не заставлял его за этим следить. Больше не
писать «остановите контейнеры, когда наиграетесь» — останавливать
самому перед концом ответа.

**Язык.** Английский README допустим, но **основным остаётся русский**.
Сделан `README.en.md` со ссылкой на русский как на главный.

**Отчёт о незапушенном.** Автор поймал Claude на устаревших сведениях:
`git log origin/main..main` читает локальную копию, которая обновляется
только при `fetch`. Перед тем как говорить, что не запушено, делать
`git fetch --all --prune` и проверять **все** ветки, включая `gh-pages`.

**Что автор хотел понять сам.** Он дважды просил объяснить простыми
словами, что представляет собой проект, какую проблему решает и
насколько польза настоящая. Объяснение было дано в переписке: проблема
реальна и подтверждена чужими данными, диагностика полезна почти любому
и ничем не рискует, маршрутизатор полезен только тем, кому важна
предсказуемость отклика, а боевого клиента у проекта нет. Эту рамку
стоит сохранять и дальше — она честная.

## Что сделано к концу 20.09: сводка

Закрыты все пункты подготовки к Венчурным играм, кроме одного.

| направление | состояние |
|---|---|
| Проверка на чужом ПО (PostgREST) | сделано |
| Проверка посылки на трассе Azure | сделано |
| Упаковка стенда в Docker | сделано |
| Модуль для nginx | сделано |
| Маршрутизатор на Go | сделано |
| Диагностика в браузере | сделано |
| Публичная страница (GitHub Pages) | ветка `gh-pages` готова |
| Сбор обезличенных отчётов | сделано |
| Разбор конкурентов | сделано |
| Экономика по публичным ценам | сделано |
| Материалы питча | сделано |
| README на английском | сделано, вторичный |
| **Живая демонстрация в сети** | **не сделано: нужен платный хостинг** |

## Что в этом направлении ещё не сделано

- Живая демонстрация маршрутизации в сети (нужен платный хостинг;
  статическая диагностика закрыта веткой `gh-pages`).

## Переименование завершено: adaptive-routing → loadlens (20.09, вечер)

Автор переименовал репозиторий на GitHub и папку на компьютере, после
чего **перестал видеть историю переписки**. Причина найдена и устранена,
и её стоит помнить: Claude Code хранит историю сессии в папке, имя
которой образовано от **пути рабочей директории**
(`~/.claude/projects/-home-winlast-projects-adaptive-routing`). После
`mv` рабочий путь стал другим, история осталась лежать под старым
именем, и список сессий в новой папке оказался пуст. Ничего не пропало:
файл сессии `8530f324-….jsonl` (27 МБ, 136 сообщений автора с 16.09)
вместе с папкой памяти скопирован в
`-home-winlast-projects-loadlens`, и теперь виден через `claude --resume`.

**Правило на будущее:** прежде чем переименовывать директорию проекта,
скопировать содержимое `~/.claude/projects/<старый-путь>` в папку,
соответствующую новому пути. Предупреждать об этом **до** того, как
советовать `mv`.

Состояние переименования проверено по факту, а не по памяти:

| проверка | результат |
|---|---|
| `origin` | `https://github.com/winlast/loadlens.git` |
| Ветки на сервере | `main`, `research`, `gh-pages` — все на месте |
| Тег `prototype-v0` | на сервере **удалён**, локальная копия осталась |
| Тег `science-v1.0` | на месте, как и задумано |
| Ветка `research` | синхронна, не тронута |

### Ссылки и QR-коды пересобраны

Шаг 6 прежнего плана выполнен. Старое имя оставалось в трёх живых
местах: `tools/build_handout.py` (обе ссылки), `loadlens/web/index.html`
и его шаблон (адрес для отправки отчёта об ошибке). Все исправлены на
`loadlens`, карточка пересобрана, оба QR-кода перегенерированы —
теперь они ведут на `https://winlast.github.io/loadlens/` и на новый
адрес репозитория. **Карточку можно печатать.**

Упоминание `adaptive-routing` в `core/workload.py` — это неизменяемая
соль для генератора нагрузки, а не ссылка. Её трогать нельзя: изменение
соли сделает прежние замеры невоспроизводимыми. Упоминания в журналах
`data/*.log` — историческая запись, тоже не трогаем.

Сверка чисел после правок: 64 утверждения, расхождений нет.

## Питч для бизнес-тренинга и ценовая политика (20.09, вечер)

Автор попросил текст выступления для бизнес-тренинга премии.
Результат — `ledentsov/ПИТЧ_ТРЕНИНГ.md`, включён в список публичных
текстов в `tools/verify_numbers.py` наравне с заявкой и README.

**Новое, чего раньше в материалах не было, — ценовая политика.** До
сих пор заявка честно говорила «модель монетизации — гипотеза, цену
никто не подтверждал», и цифр не называла. Теперь модель сформулирована
так, чтобы её можно было защищать:

| тариф | кому | цена |
|---|---|---|
| Диагностика | всем | 0 ₽, навсегда |
| Команда | до 1000 rps | 15 000 ₽/мес |
| Компания | до 5000 rps | 45 000 ₽/мес |

Цена выводится из измеренной экономии, а не назначена: на 1000 rps при
p95 ≤ 100 мс клиент экономит 54 720 ₽/мес, мы берём около четверти.
Записанное правило — **цена не превышает трети экономии, показанной на
журнале самого клиента.** Отсюда же следуют отказы: не брать за запрос,
не брать процент от оборота, не продавать тем, у кого обещание мягче
200 мс (там экономия измеренно нулевая).

Размер рынка в деньгах в питче **намеренно не назван**. Он стоит в
списке недоказанного, и выдуманное число там было бы прямым нарушением
правила проекта. Вместо цифры — шаг, который её измеряет.

## Ответ на вопрос автора: есть ли в продукте ИИ

Вопрос задан прямо и ответ должен оставаться неизменным во всех
материалах, иначе разные тексты разойдутся.

| | в продукте | где живёт |
|---|---|---|
| ИИ-агент, языковая модель | **нет, нигде** | — |
| Нейронная сеть | **нет** | `core/predictor.py`, только стенд |
| Машинное обучение | **да, регрессия** | `fit_power_law`, `fit_multi` |

Проверено по коду: в `loadlens/`, `gorouter/`, `nginx/` и `docker/` нет
ни одного упоминания torch, scikit-learn или pandas. Образ Docker
собран без них намеренно.

Нейросеть не пошла в продукт по измеренной причине: решение стоит
**165,51 мкс против 1,56 мкс** у степенного закона
(`data/decision_cost.json`), то есть в сто с лишним раз дороже, и при
тысячах запросов в секунду сама становится узким местом. *Внимание: в
Части 2 этого документа стоит 174 мкс — величина промежуточного
прогона, она внесена в запрещённые в `verify_numbers.py`. Верное
значение 165,51.*

То, что действительно работает в продукте, — метод наименьших квадратов
в логарифмах, то есть многомерная линейная регрессия, около шестидесяти
строк на чистом Python. Обучение настоящее: настройка на одной половине
журнала, проверка на другой, показатели степени выводятся из данных
(1,91 и 1,98 при истинном 2,00). Но называть это нейросетью или ИИ
нельзя.

**Замечание к названию в заявке.** `ledentsov/ЗАЯВКА.md` озаглавлена
«Интеллектуальная система адаптивной балансировки нагрузки». Слово
«интеллектуальная» — единственное место во всех материалах, которое
можно прочитать как заявку на ИИ. Название оставлено без изменений,
потому что автор не помнит, подано ли оно в заявку, и менять его —
его решение. Если оно ещё не подано, слово стоит убрать.

## Мелкий долг: число, не прослеживаемое до `data/`

README утверждает «58 наносекунд на решение» для маршрутизатора на Go.
Этого числа нет ни в одном файле `data/` и оно не проверяется
`verify_numbers.py` — то есть нарушено железное правило проекта.

Число перепроверено прогоном `go test -bench=Decide -benchmem -count=3`
в `gorouter/`: **59,2 нс/оп, ноль выделений памяти**, три прогона
подряд. Утверждение верно. Но его следует сохранить в `data/` и
добавить в сверку, иначе оно останется единственным публичным числом
без опоры. Не сделано: автор об этом не просил.

Заодно: `data/gorouter.json` хранит `стоимость_решения_нс: 1242` — это
другая величина (учёт внутри обработки запроса, а не микрозамер самой
функции), и путать их нельзя.
