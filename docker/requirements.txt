# Только то, что нужно самому стенду в работе. Обучающая часть
# (torch, scikit-learn, pandas, matplotlib) в образ не кладётся: оценки
# уже настроены, а образ из-за них раздувался бы на два гигабайта.
flask==3.0.3
fastapi==0.111.0
uvicorn[standard]==0.30.1
requests==2.32.3
httpx==0.27.0
numpy==1.26.4
pillow==10.3.0
